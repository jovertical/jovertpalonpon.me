name: Actions@api

on:
  push:
    branches:
      - master
      - "api/**" # Remove this
    # paths:
    # - 'api/**'

jobs:
  Build-Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v1

      - name: Decrypt configuration file
        run: sh ./.github/scripts/decrypt_secret.sh
        env:
          APP: api
          SECRET_PASSPHRASE: ${{secrets.API_SECRET_PASSPHRASE}}

      - name: Move files to root
        run: cp api/. .

      - name: Install PHP dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-suggest --no-progress --prefer-dist

      - name: Run the test suite
        run: composer test

      # - name: Login
      #   uses: actions/heroku@master
      #   env:
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      #   with:
      #     args: container:login

      # - name: Push
      #   uses: actions/heroku@master
      #   env:
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      #   with:
      #     args: container:push -a palonponjovertlota web

      # - name: Release
      #   uses: actions/heroku@master
      #   env:
      #     HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      #   with:
      #     args: container:release -a palonponjovertlota web